[build-system]
requires = ["setuptools>=61.0"]
build-backend = "setuptools.build_meta"

[project]
name = "tradingagents"
version = "0.3.1"
description = "TradingAgents: Multi-Agents LLM Financial Trading Framework"
readme = "README.md"
requires-python = ">=3.10"
dependencies = [
    "model2vec>=0.8",  # P2-04 semantic memory embeddings (potion-base-8M)
    "langchain-core>=0.3.81",
    "backtrader>=1.9.78.123",
    "langchain-anthropic>=0.3.15",
    "langchain-experimental>=0.3.4",
    "langchain-google-genai>=4.0.0",
    "langchain-openai>=0.3.23",
    "langgraph>=0.4.8",
    "langgraph-checkpoint-sqlite>=2.0.0",
    "pandas>=2.3.0",
    "parsel>=1.10.0",
    "python-dotenv>=1.0.0",
    "pytz>=2025.2",
    "questionary>=2.1.0",
    "redis>=6.2.0",
    "requests>=2.32.4",
    "rich>=14.0.0",
    "typer>=0.21.0",
    "setuptools>=80.9.0",
    "stockstats>=0.6.5",
    "tqdm>=4.67.1",
    "typing-extensions>=4.14.0",
    "yfinance>=1.4.1",
]

[project.optional-dependencies]
dev = [
    "ruff>=0.15",
    "pytest>=8.0",
    "pytest-subtests>=0.13",
]
# Amazon Bedrock support (AWS SigV4 auth + boto3). Optional so the core install
# stays lean: pip install "tradingagents[bedrock]".
bedrock = [
    "langchain-aws>=1.5.0",
]
# Qdrant backend for the Pro memory system (ADR-0020). The default in-memory
# index needs nothing; install this to scale retrieval beyond one process.
qdrant = [
    "qdrant-client>=1.12",
]
# Pro dashboard (Phase 10): FastAPI app + uvicorn server.
dashboard = [
    "fastapi>=0.110",
    "uvicorn>=0.30",
    # backtest run metadata store (production; file store remains the fallback)
    "google-cloud-firestore>=2.19",
]
# Institutional backtest report: matplotlib charts for the visual dashboard.
# Report-only so the core install stays lean:
# pip install "tradingagents[backtest-report]".
backtest-report = [
    "matplotlib>=3.8",
]
# Optional ML accelerators (track T3): scikit-learn (GMM regime + gradient-
# boosting meta-label) and hmmlearn (HMM regime). The pure-Python defaults
# (in-repo KMeans regime + logistic meta-label) need none of these — install
# only to enable the heavier engines: pip install "tradingagents[ml]".
ml = [
    "scikit-learn>=1.4",
    "hmmlearn>=0.3",
]
# Optional deep-RL policy (track T3, ADR-0025): a torch MLP Q-network behind
# the PolicyProtocol seam. The tabular QTablePolicy needs none of this:
# pip install "tradingagents[rl-deep]".
rl-deep = [
    "torch>=2.2",
]

[project.scripts]
tradingagents = "cli.main:app"
tradingagents-pro = "tradingagents.pro.cli:main"

[tool.setuptools.packages.find]
include = ["tradingagents*", "cli*"]

[tool.setuptools.package-data]
cli = ["static/*"]
"tradingagents.pro.agents" = ["prompts/*.md"]
"tradingagents.pro.pipeline" = ["prompts/*.md"]
"tradingagents.pro.dashboard" = ["templates/*.html", "static/*", "static/assets/*"]

[tool.pytest.ini_options]
testpaths = ["tests"]
addopts = "-ra --strict-markers"
markers = [
    "unit: fast isolated unit tests",
    "integration: tests requiring external services",
    "smoke: quick sanity-check tests",
]
filterwarnings = [
    "ignore::DeprecationWarning",
]

[tool.ruff]
line-length = 100
target-version = "py310"
extend-exclude = ["results", "worklog"]

[tool.ruff.lint]
# Standard "good defaults" rule set (pyflakes + pycodestyle + isort + bugbear +
# pyupgrade + comprehensions/simplify). Line length (E501) and layout are owned
# by the formatter; whole-repo `ruff format` adoption is deferred until the
# open-PR backlog clears, to avoid mass merge conflicts.
select = ["E", "W", "F", "I", "B", "UP", "C4", "SIM"]
ignore = ["E501"]

[tool.ruff.lint.per-file-ignores]
"**/__init__.py" = ["F401"]   # intentional re-exports
"tradingagents/pro/cli.py" = ["B008"]  # Typer Option()-in-default is idiomatic

[tool.ruff.lint.isort]
# Keep multiple aliased names from one module in a single combined import block
# (e.g. the vendor re-exports in interface.py) instead of one statement per name.
combine-as-imports = true
